# 이 파일을 .env로 복사한 뒤 사용할 AI 공급자의 키를 입력하세요.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.4-mini

# Gemini를 우선 사용하고, 실패하면 OpenAI 또는 로컬 규칙으로 대체합니다.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-1.5-flash

# 실제 배포에서는 반드시 충분히 긴 무작위 값으로 바꾸세요.
SECRET_KEY=change-me-before-production
